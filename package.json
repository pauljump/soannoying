{
  "name": "soannoying",
  "private": true,
  "version": "0.1.0",
  "description": "Open, source-tracked evidence for AI-solvable human frictions.",
  "scripts": {
    "build:pages": "node scripts/build-pages.mjs",
    "test": "node scripts/validate-release.mjs && node scripts/validate-mining-runs.mjs && node scripts/build-pages.mjs --check",
    "validate": "node scripts/validate-release.mjs && node scripts/validate-mining-runs.mjs && node scripts/build-pages.mjs --check"
  },
  "engines": {
    "node": ">=20"
  }
}
